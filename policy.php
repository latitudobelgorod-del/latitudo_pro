<?php
/**
 * Политика в отношении обработки персональных данных — ОТДЕЛЬНАЯ СТРАНИЦА.
 *
 * Зачем она нужна отдельно от всплывающего окна в подвале: 152-ФЗ ст. 18.1 требует
 * опубликовать документ в свободном доступе, то есть по обычному адресу, который
 * открывается прямой ссылкой. Модальное окно этому не отвечает — у него нет URL,
 * и проверяющий (как и поисковый робот) текста не увидит.
 *
 * Раньше по этому адресу лежал сам фрагмент текста, и прямое открытие /policy.php
 * заканчивалось фатальной ошибкой: htmlspecialcharsbx() не существует без пролога
 * Битрикса. Теперь текст переехал в /include/policy.php (правится в админке там же,
 * где остальные включаемые области), а здесь — полноценная страница.
 *
 * Один и тот же файл питает и страницу, и всплывающее окно, поэтому расхождений
 * между ними быть не может.
 */
require($_SERVER["DOCUMENT_ROOT"] . "/bitrix/header.php");
$APPLICATION->SetTitle("Политика в отношении обработки персональных данных");
?>

<section class="doc-page">
    <div class="container">
        <div class="doc-page__text">
            <?php $APPLICATION->IncludeFile(
                "/include/policy.php",
                array(),
                array("MODE" => "html", "NAME" => "Политика конфиденциальности")
            ); ?>
        </div>
    </div>
</section>

<?php require($_SERVER["DOCUMENT_ROOT"] . "/bitrix/footer.php"); ?>
