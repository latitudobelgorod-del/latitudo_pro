# Как мы работаем над сайтом Latitudo

Короткая памятка для команды. Сайт состоит из **трёх частей** — важно не путать, где что меняется.

| Часть | Где живёт | Кто и как меняет |
|---|---|---|
| **Контент** (тексты, фото, телефоны городов, разделы каталога) | База данных на общем сервере | Прямо в админке сервера, вживую |
| **Код** (шаблон, CSS, вёрстка) | Папка `/local`, в Git | Локально → `git push` → деплой на сервер |
| **Движок** `/bitrix/` | Только на серверах | Не трогаем, в Git его нет |

## Доступы

- **Сайт (dev):** http://latituty.beget.tech/
- **Админка:** http://latituty.beget.tech/bitrix/admin/
- **Репозиторий:** https://github.com/envisionxai/latitudo-landing
- **Макеты:** Figma-файл `Лендо-сайт Latitudo` (ключ `TdsV8RndMihig9YUyEJ5CF`)

## Рабочий процесс

### Контент → меняем на сервере
Тексты, картинки, телефоны и т.п. — правим **в админке сервера**. База одна на всех,
изменения сразу видны обоим. В Git контент не попадает (это нормально).

### Код → меняем через Git
1. Правим код **локально** (своя OSPanel).
2. Отправляем в GitHub:
   ```
   git add -A
   git commit -m "что сделал"
   git push
   ```
3. Выкладываем на сервер — в **Терминале Beget**:
   ```
   cd ~/latituty.beget.tech/public_html && git pull
   ```
4. Если правки шаблона не видны — сбросить кэш Битрикса
   (Админка → Настройки → Очистить кэш).

## Правила, чтобы ничего не сломать

- ⛔ **Код на сервере напрямую не редактируем** — только локально и через `git push`.
  Иначе правки затрутся при следующем `git pull`.
- ⛔ **`.htaccess` не коммитим** — он разный на локали (Apache) и на Beget.
  Эталон лежит в `.htaccess.example`.
- ⛔ **Секреты (`figma.token`, пароли) в Git не кладём** — они в `.gitignore`.
- ✅ Контент — только на сервере. Код — только через Git.
