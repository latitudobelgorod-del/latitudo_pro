<?
// Меню = якоря на блоки главной (по ТЗ и макету).
// Ссылки вида "/#anchor" — чтобы работали и с внутренних страниц.
// «Магазин в <город>» — через плейсхолдер #REGION_NAME_DECLINE_PP# (подставляется
// обработчиком OnEndBufferContent по поддомену, см. region.php). Меню кэшируется по типу,
// поэтому плейсхолдер надёжнее прямого значения: замена идёт уже в готовом HTML, после кэша.

// Состав пунктов — по макету раунда 4 (шапка 537:19144): 6 пунктов.
//
// СПИСОК ВСЕГДА ОДНОЙ ДЛИНЫ — это важно. Раньше «Цены» добавлялись условно, и меню
// разъезжалось: подписи приходили из списка одной длины, а ссылки из другой, из-за чего
// «Преимущества» вели на #catalog, «Фото» на #advantages, а у последнего пункта href
// оставался пустым. Ломалось не на всех страницах сразу, поэтому выглядело как «кэш».
// Пункты, которым на конкретной странице некуда вести, прячет JS в footer.php: меню
// одно на весь сайт, поэтому решать это на сервере нельзя в принципе.
$aMenuLinks = Array(
    // «Цены» → блок товаров. На лендинге раздела без товаров блок схлопнут (остаётся
    // только пустой якорь #catalog с data-empty) — там пункт скрывает JS.
    Array("Цены", "#catalog", Array(), Array(), ""),
    // «Преимущества» → блок #advantages, есть не на всех страницах — тоже прячет JS.
    Array("Преимущества", "#advantages", Array(), Array(), ""),
    Array("Фото", "#projects", Array(), Array(), ""),
    Array("Отзывы", "#reviews", Array(), Array(), ""),
    Array("Магазин в #REGION_NAME_DECLINE_PP#", "#visit-store", Array(), Array(), ""),
    Array("Контакты", "#contacts", Array(), Array(), ""),
);
?>
